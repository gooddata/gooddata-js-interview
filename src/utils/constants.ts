/**
 * Didnt think this was some SECRET/IMPORTANT key if not I would treat it with environment variables
 * Using for example dotenv, creating some .env file where I would keep
 * all the sensitive data of the application and add it on .gitignore
 */
export const PROJECT_ID = "xms7ga4tf3g3nzucd8380o2bev8oeknp";

export const GROSS_PROFIT_MEASURE = `/gdc/md/${PROJECT_ID}/obj/6877`;

export const DATE_ATTRIBUTE_MONTHS = `/gdc/md/${PROJECT_ID}/obj/2142`;

export const DATE_ATTRIBUTE = `/gdc/md/${PROJECT_ID}/obj/2180`;
