import { CONFIG } from '../../config';
const dateAttributeInMonths = `/gdc/md/${CONFIG.projectId}/obj/2142`;

export const BY_MONTHS = {
    visualizationAttribute:
    {
        displayForm: {
            uri: dateAttributeInMonths
        },
        localIdentifier: 'a1'
    }
}